services:
  app:
    build: .
    working_dir: /app
    command: pnpm dev
    ports:
      - "3001:3000"
    volumes:
      # live source for `node --watch`; rebuild the image after dependency changes
      - ./src:/app/src
      - sqlite_data:/app/data

volumes:
  sqlite_data:
